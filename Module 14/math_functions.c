#include<stdio.h>
#include<math.h>
#include<stdlib.h>
int main()
{
    // double x;
    // scanf("%lf",&x);
    // // int ans=floor(x);
    // // int ans=ceil(x);
    // int ans=round(x);
    // int x;
    // scanf("%d",&x);
    // // int ans=sqrt(x);
    // double ans=sqrt(x);
    // double x,y;
    // scanf("%lf %lf",&x,&y);
    // double ans=pow(x,y);
    // printf("%0.2lf\n",ans);    
    int x;
    scanf("%d",&x);
    int ans= abs(x);
    printf("%d",ans);
    return 0;
}